import type { Gap } from "@/domain/evaluation";

export function GapList({ gaps }: { gaps: Gap[] }) {
  return <section className="surface-card p-5 sm:p-6" aria-labelledby="gaps-heading"><p className="eyebrow">Highest-impact actions</p><h2 id="gaps-heading" className="mt-2 text-2xl font-semibold tracking-tight">Close the gaps</h2><ul className="mt-5 space-y-3">{gaps.length === 0 ? <li className="rounded-xl border border-[var(--border)] bg-[var(--canvas)] p-4 text-sm text-[var(--muted)]">No high-impact gaps remain.</li> : gaps.map((gap) => <li key={gap.featureKey} className="rounded-xl border border-[var(--border)] bg-[var(--canvas)] p-4"><div className="flex items-center justify-between gap-2"><span className="font-medium text-[var(--ink)]">{gap.label}</span><span className="mono-label rounded-full border border-[var(--ink)] px-2 py-1 font-bold">{Math.round(gap.priority)} priority</span></div><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{gap.question}</p><p className="mt-3 text-xs font-medium capitalize text-[var(--muted)]">{gap.reason.replace("_", " ")}{gap.evidenceRequested ? " · Evidence requested" : ""}</p></li>)}</ul></section>;
}
