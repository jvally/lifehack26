import type { CategoryIntelligence } from "@/domain/market";

export function MarketInsights({ intelligence }: { intelligence: CategoryIntelligence }) {
  const topIntents = [...intelligence.intents].sort((a, b) => b.weight - a.weight).slice(0, 3);
  return <section className="surface-card p-5 sm:p-6" aria-labelledby="market-heading"><p className="eyebrow">Market context</p><h2 id="market-heading" className="mt-2 text-2xl font-semibold tracking-tight">What shoppers ask for</h2><div className="mt-5 space-y-3">{topIntents.map((intent, index) => <div key={intent.id} className="rounded-xl border border-[var(--border)] bg-[var(--canvas)] p-4"><div className="flex items-start justify-between gap-3"><p className="font-medium text-[var(--ink)]">{intent.label}</p><span className="mono-label text-[var(--muted)]">0{index + 1}</span></div><p className="mt-2 text-sm text-[var(--muted)]">Demand signal: <span className="font-semibold text-[var(--ink)]">{intent.weight}</span></p></div>)}</div><div className="mt-7 border-t border-[var(--border)] pt-5"><p className="text-sm font-semibold">Peer reference points</p><dl className="mt-4 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-[var(--muted)]">Median price</dt><dd className="mt-1 font-semibold">S${intelligence.peerPriceMedian ?? "—"}</dd></div>{Object.entries(intelligence.peerMedians).map(([key, value]) => <div key={key}><dt className="text-[var(--muted)]">Median {key}</dt><dd className="mt-1 font-semibold">{value}</dd></div>)}</dl><p className="mt-5 text-xs text-[var(--muted)]">Aggregated query and permitted peer observations · 29 Aug 2026</p></div></section>;
}
