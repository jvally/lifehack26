import type { CategoryIntelligence } from "@/domain/market";

export function MarketInsights({ intelligence }: { intelligence: CategoryIntelligence }) {
  const topIntents = [...intelligence.intents].sort((a, b) => b.weight - a.weight).slice(0, 3);
  return <section className="surface-card p-5" aria-labelledby="market-heading"><p className="text-xs font-bold uppercase tracking-widest text-blue-700">Market context</p><h2 id="market-heading" className="mt-1 text-xl font-semibold">What shoppers ask for</h2><div className="mt-4 space-y-3">{topIntents.map((intent) => <div key={intent.id} className="rounded-xl bg-blue-50 p-3"><p className="font-medium text-slate-900">{intent.label}</p><p className="mt-1 text-sm text-slate-600">Demand signal: {intent.weight}</p></div>)}</div><div className="mt-5 border-t border-slate-200 pt-4"><p className="text-sm font-semibold">Peer reference points</p><dl className="mt-3 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-slate-500">Median price</dt><dd className="font-semibold">S${intelligence.peerPriceMedian ?? "—"}</dd></div>{Object.entries(intelligence.peerMedians).map(([key, value]) => <div key={key}><dt className="text-slate-500">Median {key}</dt><dd className="font-semibold">{value}</dd></div>)}</dl><p className="mt-4 text-xs text-slate-500">Aggregated query and permitted peer observations · 29 Aug 2026</p></div></section>;
}
